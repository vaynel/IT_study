
## 📂 디렉토리 구조 및 관리 방식

### Content 파일 위치

* **경로**: `/root/blog/content`
* **관리 방식**: 해당 폴더 하위에 각 주제별(예: `IT_study`, `algorithm`) 레포지토리를 `git clone`하여 관리합니다.
* **동작 원리**: Webhook 서버는 GitHub의 신호를 받으면 해당 하위 폴더로 이동하여 `git pull`을 수행하고, 최종적으로 `/root/blog` 경로에서 전체 빌드를 실행합니다.

---

## 🛠️ 주요 운영 명령어

배포 자동화 서버의 설정이 변경되었거나 상태를 점검할 때 사용하는 명령어입니다.

### 1. Webhook 서버 설정 반영 (Reload/Restart)

설정 파일(Python 코드 등)을 수정했을 때 서비스를 다시 로드합니다.

```bash
# 설정을 다시 불러올 때
systemctl reload quartz-webhook

# 서비스를 완전히 껐다 켜야 할 때
systemctl restart quartz-webhook

```

> **Tip**: 일반적으로 코드를 수정했다면 `restart`를, 환경 변수 등 가벼운 설정 변경이라면 `reload`를 사용합니다.

### 2. Webhook 서버 로그 실시간 모니터링

GitHub에서 보낸 신호가 잘 들어오는지, 빌드 과정에서 에러는 없는지 실시간으로 확인합니다.

```bash
journalctl -u quartz-webhook.service -f

```

* **사용 용도**: `git push` 후 자동 빌드가 시작되는지 확인하거나, 보안 토큰(Secret) 불일치 등의 에러를 디버깅할 때 사용합니다.

---

## 🚀 워크플로우 요약

1. **로컬**: 글 작성 후 특정 레포지토리(`IT_study` 등)에 Push.
2. **GitHub**: 서버의 `/webhook` 엔드포인트로 POST 요청 전송.
3. **서버**: `journalctl`로 로그를 확인하며 자동 `git pull` 및 `quartz build` 과정 모니터링.
4. **완료**: `blog.log2u.me`에서 업데이트된 내용 확인.

---